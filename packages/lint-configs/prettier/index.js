export default {
  // 是否在语句末尾添加分号
  semi: true,
  // 使用单引号代替双引号
  singleQuote: true,
  // 每行代码最大长度
  printWidth: 100,
  // 对象或数组末尾是否添加逗号
  trailingComma: 'none',
  // 箭头函数参数是否使用括号
  arrowParens: 'avoid',
  // 缩进空格数
  tabWidth: 2,
  // 使用空格缩进
  useTabs: false,
  // 对象中的空格 例如 { foo: bar }
  bracketSpacing: true,
  // 多行JSX中的>放置在最后一行的结尾，而不是另起一行
  jsxBracketSameLine: false,
  // jsx中使用单引号
  jsxSingleQuote: false,
  // 行尾换行符类型
  endOfLine: 'auto',
  // 是否在文件顶部插入一个特殊的 @format 标记
  insertPragma: true,
  // 是否要求在文件顶部有一个特殊的 @format 标记才格式化
  requirePragma: true,
  // 是否缩进Vue文件中的<script>和<style>标签内容
  vueIndentScriptAndStyle: false,
  // 换行符使用 lf
  endOfLine: 'lf',
  // 在HTML、Vue和JSX中每行强制换行
  htmlWhitespaceSensitivity: 'strict',
  // 是否格式化嵌入引用代码
  embeddedLanguageFormatting: 'auto'
};